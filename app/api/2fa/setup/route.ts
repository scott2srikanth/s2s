import {generateSecret,generateURI} from "otplib";import QRCode from "qrcode";import {eq} from "drizzle-orm";import {getPasswordUser,isSecondFactorVerified} from "../../../password-auth";import {ensureSecuritySchema,getDb} from "../../../../db";import {userSecurity} from "../../../../db/schema";import {encrypt} from "../../../security";
export async function POST(){try{const user=await getPasswordUser();if(!user)return Response.json({error:"Sign in required"},{status:401});await ensureSecuritySchema();const db=getDb(),existing=await db.select({enabled:userSecurity.enabled}).from(userSecurity).where(eq(userSecurity.userId,user.userId)).limit(1);if(existing[0]?.enabled&&!await isSecondFactorVerified(user.displayName))return Response.json({error:"Verify your current authenticator before replacing it"},{status:403});const secret=generateSecret({length:20}),uri=generateURI({issuer:"S2S Studio",label:user.displayName,secret}),qr=await QRCode.toDataURL(uri,{margin:1,width:240,color:{dark:"#151b25",light:"#ffffff"}}),encrypted=await encrypt(secret);await db.insert(userSecurity).values({userId:user.userId,email:user.email,encryptedSecret:encrypted,enabled:false}).onConflictDoUpdate({target:userSecurity.userId,set:{email:user.email,encryptedSecret:encrypted,enabled:false,updatedAt:new Date()}});return Response.json({qr,secret})}catch{return Response.json({error:"Unable to create authenticator setup"},{status:500})}}
